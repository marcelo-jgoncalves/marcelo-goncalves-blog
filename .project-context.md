# .project-context.md — Blog Marcelo Gonçalves
> Memória viva do projeto. Atualizar sempre que houver mudança relevante.  
> Última atualização: 2026-04-26 — Encerramento Sessão 4

---

## ⚡ PRÓXIMA SESSÃO — Comece aqui

### Branch de trabalho: `develop`
Sessões 1–3 foram feitas no `main` por engano. Em 2026-04-26 o merge `main→develop` foi feito (commit `3721093`). **Sempre trabalhar em `develop`. `main` = snapshot estável.**

### Foco: apenas ambiente `dev`
Não há ambiente de produção. Toda infra, pipeline e discussão são referentes ao ambiente `dev`.

### Estado do Terraform — PRONTO PARA APPLY
O state foi recuperado da outra máquina e está no S3:
- **Bucket state:** `marcelo-goncalves-blog-dev-tfstate` (us-east-1)
- **Key:** `blog/terraform.tfstate`
- **Lock table:** `marcelo-goncalves-blog-dev-tflock`
- **`backend.hcl`:** já existe localmente em `infra/backend.hcl` (gitignored)
- **`terraform init`:** já executado, backend configurado

O `terraform plan` roda limpo (**32 add, 14 change, 13 destroy**). Para aplicar:

```bash
# 1. Buildar as Lambdas reais (ZIPs placeholder existem só para plan)
cd backend && npm ci && npm run build   # gera infra/builds/*.zip

# 2. Apply
cd ../infra
AWS_PROFILE=claude-dev terraform apply -var-file=env/dev.tfvars
```

### O que o `terraform apply` vai fazer
| Ação | Recursos | Motivo |
|---|---|---|
| **+32 criar** | 4 novas roles IAM (least-privilege), Lambda `adminCategorias`, Lambda `postScheduler`, EventBridge Scheduler, CloudWatch Log Groups com retenção, OAC Lambda, CloudFront permission | Melhorias das sessões 1-4, nunca deployadas |
| **~14 atualizar** | Lambdas (nova role), CloudFront (adiciona OAC Lambda), Lambda URL (`NONE`→`AWS_IAM`), API GW admin/categorias | Correções de segurança |
| **-13 destruir** | Role/policy antigas monolíticas (`lambda-role`, `lambda-policy`, `upload-policy`) | Substituídas pelas novas roles granulares |

### Infra já ao vivo na AWS (não tocar sem Terraform)
- DynamoDB: `posts`, `autores`, `categorias` (dados reais)
- 9 Lambdas (versão antiga — serão atualizadas pelo apply)
- 2 CloudFront distributions + API Gateway + Cognito User Pool

### GitHub Actions — CONFIGURADO
- **OIDC Provider:** `arn:aws:iam::975707451904:oidc-provider/token.actions.githubusercontent.com` ✅
- **Role:** `GITHUB-ROLE-FOR-MARCELO-TECH-BLOG` — corrigida em 2026-04-26 (era `marcelogoncalves-tech`, agora aponta para `marcelo-goncalves-blog`, todos os branches)
- **Secrets:** `AWS_ROLE_ARN_DEV` e `AWS_ROLE_ARN_PROD` configurados no repositório ✅
- **GitHub Environment "production":** ainda não criado (não urgente — só ambiente dev por ora)

---

## 1. Arquitetura Atual (Real)

### Estrutura do Monorepo
```
marcelo-goncalves-blog/
├── frontend/           # Next.js 16 + OpenNext v3 (Blog Público)
├── backend/            # Node.js 20 + TypeScript (9 Lambdas)
├── admin/              # Vue 3 + Vite + Pinia + Amplify (CMS Interno)
├── infra/              # Terraform (IaC AWS)
│   ├── modules/        # api-gateway, cognito, dynamodb, frontend, admin, media, lambda
│   ├── env/            # dev.tfvars (log_retention=7)
│   ├── backend.hcl.example  # Template → copiar para backend.hcl (gitignored)
│   └── builds/         # ZIPs das Lambdas (gitignored, gerados pelo build)
├── scripts/
│   ├── bootstrap-state.sh      # Cria S3 + DynamoDB para TF state (já executado)
│   ├── build-placeholders.sh   # ZIPs placeholder para terraform plan local
│   └── terraform-import.sh     # Script de import emergencial (se state se perder)
├── docs/               # Blueprint, Auditoria, Contratos
└── .github/workflows/
    ├── deploy.yml      # CI — npm audit, jest (backend), terraform fmt/validate
    └── cd.yml          # CD — build + terraform + S3 + CF
```

### 9 Lambda Functions
| Lambda | Role IAM (nova, pós-apply) | Trigger |
|---|---|---|
| `getPost` | public-lambda-role | API Gateway GET /post/{slug} |
| `getPosts` | public-lambda-role | API Gateway GET /posts/*, /artigos, /categoria/{slug}, /busca, /projeto |
| `getAuthor` | public-lambda-role | API Gateway GET /autor/{id} |
| `adminPosts` | admin-lambda-role | API Gateway ANY /admin/posts + /admin/post/{slug} |
| `adminAuthors` | admin-lambda-role | API Gateway ANY /admin/autor/{id} |
| `adminCategorias` | admin-lambda-role | API Gateway ANY /admin/categorias + /admin/categorias/{slug} |
| `mediaUpload` | media-upload-role | API Gateway POST /admin/media/upload-url |
| `imageProcessor` | processor-role | S3 ObjectCreated no bucket de uploads |
| `postScheduler` | scheduler-lambda-role | EventBridge Scheduler rate(15 minutes) |

### Pipeline de Deploy (cd.yml)
```
Push develop  →  Deploy Dev (auto, após build)
Push main     →  Terraform Plan Prod → Aprovação Manual → Deploy Prod
```

---

## 2. Decisões Técnicas Críticas

### 2.1 AWS Profile Local
**`claude-dev`** para TODA operação AWS local.

### 2.2 DynamoDB: Booleanos como Números
`e_popular` e `e_projeto` são `Number (0/1)` — DynamoDB não suporta Boolean como chave de GSI.

### 2.3 Next.js: Params como Promise
`await params` ANTES de desestruturar em qualquer `page.tsx`.

### 2.4 Sharp.js: Build Especial para Lambda Linux
`backend/build.js` usa `npm install --os=linux --cpu=x64`. **NÃO alterar.**

### 2.5 OpenNext v3: Caminho de Output
ZIP do Next.js: `frontend/.open-next/server-functions/default/` (**PLURAL** `server-functions`).

### 2.6 NEXT_PUBLIC_API_URL: Runtime, Não Build
- Valor real: injetado via Lambda env var pelo Terraform
- No build CI: placeholder `https://build-placeholder.local`
- `lib/api.ts`: validação dentro de `getApiUrl()` (não em module load)

### 2.7 Admin: Build APÓS Terraform Apply
Vue.js usa `VITE_*` vars baked no build — pipeline builda admin APÓS terraform apply.

### 2.8 Logging Estruturado
Todas as 9 Lambdas emitem JSON via `backend/src/common/logger.ts`.  
`LOG_LEVEL`: `DEBUG` (dev), `INFO` (prod).

### 2.9 Terraform Remote State
Backend S3: `marcelo-goncalves-blog-dev-tfstate/blog/terraform.tfstate`  
Lock: DynamoDB `marcelo-goncalves-blog-dev-tflock`  
Profile local: `claude-dev` (declarado em `infra/backend.hcl`).

### 2.10 PostSchedulerLambda — ConditionExpression Anti-Race
`ConditionExpression: "#status = :programado"` evita double-publish.

### 2.11 CloudWatch Log Groups — Ordem de Criação
`depends_on = [aws_cloudwatch_log_group.*]` garante grupo criado antes da Lambda.

### 2.12 CORS Admin — ADMIN_ORIGIN via Terraform
Lambdas admin recebem `ADMIN_ORIGIN = "https://${module.admin.cloudfront_url}"`. Fallback `*` para dev local.

### 2.13 Lambda Function URL — OAC + AWS_IAM
`authorization_type = "AWS_IAM"` + OAC `lambda_oac` (SigV4).  
**Na AWS atual:** ainda `NONE` (será corrigido pelo `terraform apply`).

### 2.14 HTML Sanitization — sanitize-html
`backend/src/common/sanitizer.ts` aplicado em `savePost()` antes de persistir.

### 2.15 adminCategorias — Reutiliza admin-lambda-role
`categorias_table_arn` adicionado ao `admin-lambda-policy`.

### 2.16 categoriesApi no EditorView — Fallback Silencioso
Se API falhar, usa 6 categorias hardcoded. Seed via `POST /admin/categorias` após deploy.

### 2.17 FontAwesome — NPM, Não CDN
`import "@fortawesome/fontawesome-free/css/all.min.css"` em `layout.tsx`.

### 2.18 Testes Backend (Jest)
- Config: `backend/jest.config.js` com `ts-jest`
- 47 testes em 4 suites: `getPost`, `getPosts`, `adminPosts`, `postScheduler`
- Rodar: `cd backend && npm test`

### 2.19 Pagination Limit Parametrizado (Sessão 4)
Todos os endpoints públicos aceitam `?limit=N`:
- Home/recentes: default 6 | Artigos/Categoria/Busca: default 9 | Projeto: default 8

### 2.20 GitHub Actions Role
Nome: `GITHUB-ROLE-FOR-MARCELO-TECH-BLOG`  
ARN: `arn:aws:iam::975707451904:role/GITHUB-ROLE-FOR-MARCELO-TECH-BLOG`  
Trust: `repo:marcelo-jgoncalves/marcelo-goncalves-blog:ref:refs/heads/*` (todos os branches)

---

## 3. Estado da Pipeline

### deploy.yml (CI — Validação) ✅
- npm audit + lint + Jest (47 testes) + terraform fmt/validate + trivy

### cd.yml (CD — Deploy) ⚠️ Quase pronto
- Build ✅ | Terraform apply Dev ❌ aguardando `terraform apply` manual inicial
- **Bloqueante:** o primeiro apply deve ser manual (ZIPs reais + secrets já configurados)
- Após primeiro apply manual, o CD funciona automaticamente no push para `develop`

---

## 4. Backlog Técnico

### 🟠 Próximo (alto impacto)
1. **Build real das Lambdas + primeiro `terraform apply`** — fecha todos os drifts de segurança
2. **Seed de categorias no DynamoDB** — após apply, via `POST /admin/categorias`

### 🟡 Baixa Prioridade (polish)
3. Paginação bidirecional (`Pagination.tsx`) — limitação DynamoDB
4. WAF no Admin CloudFront — quando houver tráfego
5. Cognito: migrar `USER_PASSWORD_AUTH` → SRP
6. Hero height padronizado em todas as páginas
7. Layout "e_popular" no admin
8. Preview de imagens no admin

---

## 5. Dependências Críticas

| Componente | Versão | Notas |
|---|---|---|
| Next.js | 16.0.3 | `params` é Promise — sempre `await params` |
| OpenNext | ^3.1.3 | Output: `server-functions/default/` (PLURAL) |
| Tiptap | 2.11.0 (admin) | Fixado em v2 — NÃO migrar para v3 |
| AWS Amplify | ^6.15.8 | Auth via `aws-amplify/auth` |
| Sharp | ^0.33.2 | Build com flags Linux |
| esbuild | ^0.27.0 | `format: 'cjs'` obrigatório para Lambda |
| Jest | ^29.7.0 | ts-jest + `tsconfig.test.json` separado |
| sanitize-html | ^2.x | Bundled no adminPosts.zip |
| FontAwesome | 7.1.0 (npm) | `@fortawesome/fontawesome-free/css/all.min.css` |
| Terraform | ~1.14 | Remote state S3 configurado |

---

## 6. Contexto de Produto

- **Owner:** Marcelo Gonçalves — PM/Arquiteto, IA como Staff Engineer
- **Propósito:** Blog sobre IA, AWS, DevOps — AdSense + leads para consultoria
- **Ambiente:** Apenas `dev` ativo. Produção não existe ainda.
- **Estado:** Engenharia MVP completa. Infra ao vivo mas desatualizada. Próximo passo = terraform apply.
- **Fonte da Verdade:** Código > `.project-context.md` > `docs/audit-report.md` > `docs/blueprint.md`

---

## 7. Histórico de Sessões

| Sessão | Data | Principais entregas |
|---|---|---|
| 1 | Jan 2026 | Remote state, IAM least privilege, status filter API, CI/CD pipeline |
| 2 | Jan 2026 | PostScheduler, CloudWatch log groups, 47 testes Jest, structured logging |
| 3 | Fev 2026 | HTML sanitization, adminCategorias, CORS restrito, OAC Lambda, FontAwesome npm, a11y |
| 4 | Abr 2026 | Merge main→develop, state recovery, GitHub role fix, pagination limit, metadados do site |
