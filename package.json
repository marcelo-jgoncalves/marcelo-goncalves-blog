{
  "name": "mgoncalves-editorial-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo: Marcelo Goncalves Editorial Platform (frontend, backend, admin, infra)",
  "workspaces": [
    "packages/contracts",
    "backend",
    "frontend",
    "admin"
  ],
  "scripts": {
    "build:backend": "npm run build --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "build:admin": "npm run build --workspace admin"
  }
}
