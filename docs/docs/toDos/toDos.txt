Ajustes a fazer no futuro:

Em frontend/app/layout.tsx:

- ajustar:

	title: "IA Decifrada | Marcelo Gonçalves",
  	description: "Blog de autoridade sobre IA, AWS e Engenharia de Software."

Em frontend/components/layout

- ajustar:

	contato@iadecifrada.com

No dynamoDB

- ajustar:

	Texto da bio

No Dashboard

- ajustar botão do dash e cores das fontes

Na Página de postagem

- Ajustar "é popular"

Qto às Imagens:

- Gerar versão para mobile

No admim

- Uma vez logado, eu fico logado pra sempre na minha máquina a menos que entre em uma aba anônima

No header

- corrigir cor da borda do skip button

No seção do adsense

- retirar fundo cinza e linha

No admin

- corrigir layout de "e_popular"
- corrigir problema de preview de Imagens

Na página de Artigos

- melhorar botões de "próximo"

Na página d'O Projeto

- melhorar espaçamento do banner adsense

Todas as Páginas:

- Padronizar a altura do hero

adsense

- Padronizar banners adsense do topo e do meio das postagens.

Número de postagens por Página

TO-DO: Padronização de Paginação e Limites de Posts
Objetivo: Garantir que o frontend dite a quantidade exata de posts retornados pelo DynamoDB para otimizar a performance, UX e adequar a quantidade de cards aos layouts (grids vs. listas).

1. Ajustes no Frontend (Next.js)
Arquivo: frontend/lib/api.ts
Ação: Refatorar as funções de listagem para aceitar um parâmetro limit e enviá-lo via Query String (?limit=X&nextToken=Y).

Página "O Projeto" (Timeline): * Função: getProjectPosts

Limite Ideal: 8 posts.

Home (Vitrine): * Função: getRecentPosts

Limite Ideal: 6 posts (forma um grid perfeito de 3x2 no desktop).

Página de Artigos e Categorias (Catálogo): * Funções: getAllPosts e getPostsByCategory

Limite Ideal: 9 ou 12 posts (forma grids perfeitos de 3x3 ou 3x4).

Resultados de Busca: * Função: searchPosts

Limite Ideal: 9 ou 12 posts.

Exemplo de implementação base (Frontend):

export async function getAllPosts(nextToken?: string, limit: number = 9) {
  const params = new URLSearchParams();
  if (nextToken) params.set('nextToken', nextToken);
  params.set('limit', limit.toString()); // <- Injeção do limite

  const res = await fetch(`${API_URL}/artigos?${params.toString()}`, { next: { revalidate: 60 } });
  // ...
}

2. Ajustes no Backend Serverless (AWS Lambda)
Arquivos: Códigos das suas funções Lambda (Node.js/Python) que atendem os endpoints /projeto, /posts/recentes, /artigos, etc.
Ação: Ler o limit da URL e repassar para os parâmetros de busca do DynamoDB.

O que deve ser feito no código do Lambda:

Capturar o valor: event.queryStringParameters.limit.

Converter para número inteiro (parseInt).

Definir um fallback de segurança (ex: se não vier limite na URL, assume 10).

Injetar o valor na chave Limit dos parâmetros do DynamoDB.DocumentClient (Query ou Scan).

Exemplo de implementação base (Backend AWS / Node.js):

exports.handler = async (event) => {
  // 1. Pega da URL ou usa 10 como padrão
  let dbLimit = 10;
  if (event.queryStringParameters && event.queryStringParameters.limit) {
    dbLimit = parseInt(event.queryStringParameters.limit, 10);
  }

  const params = {
    TableName: process.env.TABLE_NAME,
    Limit: dbLimit, // <- Passa o limite pro DynamoDB
    // ... outros parâmetros (IndexName, ExclusiveStartKey para o nextToken, etc.)
  };

  const result = await dynamoDb.query(params).promise();
  // ...
};

--- Ajustar código para tables